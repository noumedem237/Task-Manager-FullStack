# Task Manager — Test de recrutement

Monorepo : `backend/` (Spring Boot), `frontend/` (React + Vite + TS), `mobile/` (Flutter, bonus).

## Structure
```
task-manager/
├── backend/     # API Spring Boot (JWT, JPA, MySQL)
├── frontend/    # React + Vite + TSX
├── mobile/      # Flutter (bonus)
└── .github/workflows/  # CI/CD (bonus)
```

## Statut d'avancement
- [ ] Backend : auth (register/login JWT)
- [ ] Backend : CRUD tasks
- [ ] Frontend : auth UI
- [ ] Frontend : CRUD UI + filtres/recherche
- [ ] Mobile (bonus)
- [ ] CI/CD + déploiement GCP (bonus)

## Installation
(à compléter au fur et à mesure du développement)
